# Bright Health API

Health data ingestion and retrieval service built with FastAPI.

## Setup

```bash
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

## Run Locally

```bash
uvicorn app.main:app --reload
```

## Run Tests

```bash
pytest
```
